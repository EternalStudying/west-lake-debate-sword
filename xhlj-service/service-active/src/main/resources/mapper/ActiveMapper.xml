<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.swyxl.active.mapper.ActiveMapper">

    <sql id="columns">
        id, name, des, year ,month, day, ac_code, location, image, price, enrollment, participant, is_deleted, create_time, update_time
    </sql>

    <update id="addLike">
        update active set like_count = like_count + 1 where id = #{id}
    </update>

    <update id="addEnrollment">
        update active set enrollment = enrollment + 1 where id = #{activeId}
    </update>

    <select id="selectAll" resultType="com.swyxl.model.entity.service.active.Active">
        select <include refid="columns"/> from active where is_deleted = 0
    </select>

    <select id="selectByUserId" resultType="com.swyxl.model.entity.service.active.Active">
        select a.* from active a
        left join user_active ua on a.id = ua.active_id
        where a.is_deleted = 0 and ua.is_deleted = 0 and ua.user_id = #{userId}
    </select>

    <select id="selectById" resultType="com.swyxl.model.entity.service.active.Active">
        select <include refid="columns"/> from active where id = #{id} and is_deleted = 0
    </select>
</mapper>